<!--<Window x:Class="SFolder.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SFolder"
        mc:Ignorable="d"
        Title="SFolder" Height="450" Width="470">
    <Window.Resources>
        <ResourceDictionary Source="/WPF/Resource/Styles.xaml" />
    </Window.Resources>
    <TabControl>
        <TabItem Header="Update Revision">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="100"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="30"/>
                    <RowDefinition Height="250"/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <TextBlock Text="Source Folder:" Grid.Row="0"/>
                <TextBox Text="{Binding SourcePath}" Grid.Row="0" Grid.Column="1"/>
                <Button Content="..." Grid.Row="0" Grid.Column="2" Command="{Binding BrowseCommand}"/>

                <TextBlock Text="Target Folder:" Grid.Row="1"/>
                <TextBox Text="{Binding TargetPath}" Grid.Row="1" Grid.Column="1"/>
                <Button Content=".." Grid.Row="1" Grid.Column="2" Command="{Binding BrowseCommand2}"/>


                <StackPanel Grid.Row="2" Grid.ColumnSpan="3" Grid.RowSpan="2">
                    <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="5">
                        <StackPanel>
                            <Button HorizontalAlignment="Center" Content="Add Multi Target Folder" Command="{Binding ExtractCommand}" Width="180"  FontWeight="Bold"/>
                            <ScrollViewer>
                                <TextBox Text="{Binding PdfPath}" Width="400" Height="200" Focusable="False"/>
                            </ScrollViewer>
                        </StackPanel>
                    </Border>
                    <Button Content="Combine" Command="{Binding CombineCmd}"/>
                    <Button Content="Check PDF/DWG" Width="120"/>
                </StackPanel>
            </Grid>
        </TabItem>
    </TabControl>
    
</Window>-->
    
    
<Window x:Class="SFolder.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:RevisionFileUpdater.ViewModels"
        Title="Revision File Updater"
        Height="220" Width="550"
        ResizeMode="NoResize">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="100"/>
        </Grid.ColumnDefinitions>

        <!-- Source -->
        <TextBox Grid.Row="0"
                 Text="{Binding SourcePath,
                        UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,10,10"/>

        <Button Grid.Row="0"
                Grid.Column="1"
                Content="Source"
                Command="{Binding BrowseSourceCommand}"
                Margin="0,0,0,10"/>

        <!-- Target -->
        <TextBox Grid.Row="1"
                 Text="{Binding TargetPath,
                        UpdateSourceTrigger=PropertyChanged}"
                 Margin="0,0,10,10"/>

        <Button Grid.Row="1"
                Grid.Column="1"
                Content="Target"
                Command="{Binding BrowseTargetCommand}"
                Margin="0,0,0,10"/>

        <!-- Execute -->
        <Button Grid.Row="2"
                Grid.ColumnSpan="2"
                Content="Execute"
                Height="35"
                Command="{Binding ExecuteCommand}"/>
    </Grid>
</Window>